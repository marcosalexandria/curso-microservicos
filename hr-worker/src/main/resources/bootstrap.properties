spring.application.name=hr-worker

#para o projeto Worker saber a localizacao do servidor de configuracao
spring.cloud.config.uri=http://localhost:8888

#definindo o perfil a ser usado
spring.profiles.active=test

#Para permitir que o actuator atualize todas as configuracoes em runtime
management.endpoints.web.exposure.include=*